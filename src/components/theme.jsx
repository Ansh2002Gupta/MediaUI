import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0061FF",
      light: "#4AE1F1",
    },
    secondary: {
      main: "#000000",
      light: "#FFFFFF",
    },
    other: {
      main: "#D3B88C",
    },
  },
});
